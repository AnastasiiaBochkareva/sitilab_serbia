mixin Subscribe(param={})
  .subscribe
    .subscribe__title.title-sm Подписаться на рассылки
    .subscribe__cell
      +Input({ type: 'text', placeholder: 'Имя' })
      +Input({ type: 'text', placeholder: 'E-mail*' })
      button.btn.btn_secondary Подписаться
    .subscribe__cell
      .subscribe__mark * Поля, обязательные для заполнения
      .subscribe__check   
        +Checkbox({
            text: 'Я согласен на получение ',
        })
        a(href="#").subscribe__link.text-blue рекламно-информационных рассылок 
        
